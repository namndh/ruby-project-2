<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Rails Devise" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Music is life" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header >
      <%= render 'layouts/navigation' if user_signed_in? %>
      <%= render 'layouts/login' if !user_signed_in? %>
    </header>
    <main role="main" style="min-height:100%;">

       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
